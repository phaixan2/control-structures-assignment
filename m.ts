class MyHashSet {
  constructor() {}

  add(key: number): void {
    this.key = key;
  }

  remove(key: number): void {}

  contains(key: number): boolean {}
}

/**
 * Your MyHashSet object willbe instantiated and called as such:
 * var obj = new MyHashSet()
 * obj.add(key)
 * obj.remove(key)
 * var param_3 = obj.contains(key)
 */
console.log();
